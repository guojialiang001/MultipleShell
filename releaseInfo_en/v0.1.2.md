1. Claude Code sessions use the `C:\Users\<username>\.claude.json` config regardless of whether CC Switch is enabled. (If it doesn't exist, create one.) Copy `.claude.json` only; do not copy `.claude.json.backup`.
2. Add a “soft link `.claude.json`” switch: when enabled, validate that the system points Claude Code to the home `.claude.json`; when disabled, copy `.claude.json` following rule #1.
3. Defaults on first run: “Only CC Switch configs” is OFF; “Auto-detect” is OFF.
4. Check whether CC Switch exists under the home directory. If it doesn’t exist, do not error; show a “CC Switch not found” hint instead.
5. Move the “soft link `.claude.json`” switch into Template Management → Claude Code, above the available templates list.
