1.Claude Code 会话无论是否启用 CCSWITCH，都统一使用 C 盘主目录的 .claude.json 配置。(如果没有新建一份)，复制.claude.json  不复制.claude.json.backup    
2.增加软引用.claude.json  开关，如果开启，是会校验当前系统引用CLAUDECODE 的主目录.claude.json如果不开启。复制.claude.json  遵循第一条。
3.配置项默认初始化的时候，默认为不只读 CCSWITCH。默认为非自动检测。
4.校验主目录是否存在CCSWITCH,不存在，不报错，给一个不存在CCSWITCH的提示
5.软引导.claude.json开关放到 配置管理中，Claudecode分类下，在可用配置上方
