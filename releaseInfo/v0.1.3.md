1.增加设置中 关闭调节开关，输入CLOSE或者双击确认弹窗 （输入和双击确认。）
2.选取CCSwitch配置之后，新增非CCSwitch终端，发现CCSwitch还是使用的是CCSwitch配置。
3.调整样式。
### 滑块对齐修复
- ConfigSelector.vue ：修复了配置选择器中的滑块对齐
  
  - 使用 top: 50% + transform: translateY(-50%) 确保滑块按钮垂直居中
  - 增大了滑块尺寸（44x24px）提高可点击区域
  - 优化了滑块轨道和按钮的比例
- MenuBar.vue ：修复了设置面板中的关闭模式滑块对齐
  
  - 应用相同的居中技术确保视觉一致性
### 整体舒适度优化
- 模态框 ：优化了背景模糊效果（8px）和动画过渡
- 按钮交互 ：添加了悬停时的上移效果（ translateY(-1px) ）和点击反馈
- 颜色对比度 ：微调了透明度值，在保持暗色主题的同时提高可读性
- 动画曲线 ：所有过渡使用 ease 曲线，更符合人体视觉舒适度
- 间距优化 ：增加了元素间距，提高呼吸感

4 不做 hardlink/symlink（只 copy .claude.json 到对应 claude-homes/<id>）
  - 启动会话时把 USERPROFILE/HOME 指向这个 claude-homes/<id>（至少对 CC Switch 模板这样做
  把“软引用开关”从 UI/逻辑里移除。

5.只复制 .claude.json 不会带上 Claude Code 的“会话/项目历史”,这个会话历史只能保存到单个配置所属文件夹中。
6.复制.claude.json时复制过来。删除 当前文件的所有lastSessionId，并保证写回的是合法 JSON  删除 当前文件的projects