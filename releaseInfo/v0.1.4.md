
1.复制.claude.json时,配置目录文件夹中保留history.jsonl
2.UI，选择配置中,把工作目录提到最顶部
3.UI  设置中 应用改为关闭提示
4.终端会话中，一旦用户目录更改，通过解析指令触发，后置更新当前终端用户目录。
5. Claude Code：默认保留每模板 profile 下的 `history.jsonl`；如需从零开始，设置 `MPS_CLAUDE_CLEAR_HISTORY=1`。
6. Codex：新增 per-template 持久化目录 `<userData>/codex-runtime/<configId>/persist/`，会话启动/退出同步白名单文件（默认 `history.jsonl`），并用 lock + delta 追加避免并发覆盖。
   - `MPS_CODEX_CLEAR_HISTORY=1`：按白名单清理持久化历史/状态
   - `MPS_CODEX_PERSIST_WHITELIST=...`：自定义白名单（逗号/分号分隔）
7. OpenCode：在配置编辑器与文档中提示：工作目录存在本地 `./.opencode.json` 时，上游 OpenCode 会合并并可能覆盖全局配置。
8. 文档：更新中英文 README 与工具历史留存指南，统一开关与默认行为。

 